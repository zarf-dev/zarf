mirrors:
  registry.dso.mil:
    endpoint:
      - "https://localhost"
  registry1.dso.mil:
    endpoint:
      - "https://localhost"
  docker.io:
    endpoint:
      - "https://localhost"
  registry-1.docker.io:
    endpoint:
      - "https://localhost"
