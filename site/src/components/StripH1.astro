---
let html = await Astro.slots.render("default");

// remove h1 from the html
html = html.replace(
  /<div\s+class="sl-heading-wrapper level-h1">[\s\S]*?<\/div>\s*/i,
  ""
);
---

<Fragment set:html={html} />
