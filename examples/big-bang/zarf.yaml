kind: ZarfPackageConfig
metadata:
  name: big-bang-example
  description: "Deploy Big Bang Core"
  version: 1.53.0
  url: https://p1.dso.mil/products/big-bang
  # Big Bang / Iron Bank are only amd64
  architecture: amd64

variables:
  - name: DOMAIN
    default: "bigbang.dev"
    prompt: false

components:
  - name: bigbang
    required: true
    extensions:
      bigbang:
        version: 1.53.0
        valuesFrom:
          - config/ingress.yaml
          - config/kyverno.yaml
          - config/loki.yaml
          - config/neuvector.yaml
          - config/patch-images.yaml
          # Values are merged in order, so this will override the above and disable everything
          - config/disable-all.yaml
