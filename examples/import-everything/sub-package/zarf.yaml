kind: ZarfPackageConfig
metadata:
  name: sub-package
  version: 0.0.1

components:
  - name: jeff
    required: true
    actions:
      onCreate:
        before:
          - cmd: echo "⏲️ Preparing things for `zarf package create`..."
        after:
          - cmd: echo "🧹 Cleaning up after `zarf package create`..."
