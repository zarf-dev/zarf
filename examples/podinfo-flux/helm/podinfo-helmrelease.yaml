apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: flux-system
spec:
  interval: 5m0s
  chart:
    spec:
      chart: podinfo
      version: '6.4.0'
      sourceRef:
        kind: HelmRepository
        name: podinfo
      interval: 30s
  targetNamespace: podinfo-helm
