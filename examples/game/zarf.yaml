kind: ZarfPackageConfig
metadata:
  name: appliance-demo-doom
  description: "Demo Zarf appliance mode with doom game"

components:
  - name: baseline
    required: true
    manifests:
      - name: doom
        files:
          - manifests/ingress.yaml
          - manifests/deployment.yaml
          - manifests/service.yaml
    images:
      - defenseunicorns/zarf-game:doom
