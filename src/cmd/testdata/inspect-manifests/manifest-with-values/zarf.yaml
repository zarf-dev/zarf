kind: ZarfPackageConfig
metadata:
  name: manifest-with-values

components:
  - name: httpd
    manifests:
      - name: httpd-manifest
        files:
          - deployment.yaml
        template: true
