# Test Collision Package

This package is used to test that publishing a package works even when a directory
with the same name as the package exists in the current working directory.

See issue: https://github.com/zarf-dev/zarf/issues/4148
