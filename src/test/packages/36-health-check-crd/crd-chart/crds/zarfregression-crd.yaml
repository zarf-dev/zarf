apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: zarfregressions.test.zarf.dev
spec:
  group: test.zarf.dev
  scope: Namespaced
  names:
    kind: ZarfRegression
    listKind: ZarfRegressionList
    plural: zarfregressions
    singular: zarfregression
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
