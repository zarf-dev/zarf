metadata:
  name: bundle
  description: a bundle
  version: 0.0.1
  # architecture: amd64

packages:
  - repository: localhost:888/init
    ref: 0.0.1
    components:
      - k3s
      - injector
  - repository: localhost:889/remote-manifests
    ref: 0.0.1
    components:
      - "*" # grab all components
