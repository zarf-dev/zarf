apiVersion: zarf.dev/v1beta1
kind: ZarfPackageConfig
metadata:
  name: component-remote-import
components:
  - name: imported-component
    import:
      remote:
        - url: oci://[[ .registryURL ]]/published-component:0.0.1
