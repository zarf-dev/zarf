kind: ZarfPackageConfig
metadata:
  name: namespace-labels-manifests
  description: Simple example to load classic DOS games into K8s in the airgap

components:
  - name: baseline
    required: true
    manifests:
      - name: multi-games
        namespace: namespace-labels-manifests
        namespaceLabels:
          testlabelkey: testlabelvalue
        files:
          - ../../../../examples/dos-games/manifests/deployment.yaml
          - ../../../../examples/dos-games/manifests/service.yaml
    images:
      - defenseunicorns/zarf-game:multi-tile-dark
    actions:
      onDeploy:
        after:
          - wait:
              cluster:
                kind: deployment
                name: game
                namespace: namespace-labels-manifests
                condition: available
