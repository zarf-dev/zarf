kind: ZarfPackageConfig
metadata:
  name: test-values-schema
  description: Test package for values schema validation

values:
  files:
    - values.yaml
  schema: values.schema.json

components:
  - name: test-schema
    description: Component to test schema validation
    required: true
    manifests:
      - name: test-configmap
        template: true
        files:
          - configmap.yaml
