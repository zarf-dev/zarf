kind: ZarfInitConfig
metadata:
  name: init
  description: Used to establish a new Zarf cluster

components:
  - name: zarf-agent
    required: true
    import:
      path: ../../../../packages/zarf-agent
