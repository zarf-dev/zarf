apiVersion: v1
kind: ConfigMap
metadata:
  name: pre-install-hook-config
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-5"
data:
  message: "This was created by a pre-install hook"
