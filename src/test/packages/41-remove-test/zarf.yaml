kind: ZarfPackageConfig
metadata:
  name: remove-test

components:
  - name: first
    required: true
    manifests:
      - name: pod
        namespace: first
        files:
          - pod.yaml
    images:
      - ghcr.io/zarf-dev/images/alpine:3.21.3

  - name: second
    required: true
    images:
      - ghcr.io/zarf-dev/images/alpine:3.21.3
    manifests:
      - name: pod
        namespace: second
        files:
          - pod.yaml
