kind: ZarfPackageConfig
metadata:
  name: parent-package
values:
  files:
    - parent-values.yaml
  schema: "parent-values.schema.json"
components:
  - name: imported-component
    required: true
    import:
      path: import
