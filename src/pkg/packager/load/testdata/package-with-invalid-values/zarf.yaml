kind: ZarfPackageConfig
metadata:
  name: test-invalid-values-schema
  description: Test package with invalid values that should fail schema validation

values:
  files:
    - values/values.yaml
  schema: values.schema.json

components:
  - name: test-component
    required: true
    manifests:
      - name: test-manifest
        files:
          - manifest.yaml
