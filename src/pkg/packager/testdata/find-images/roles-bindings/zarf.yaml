kind: ZarfPackageConfig
metadata:
  name: git-helm-repo
components:
  - name: baseline
    required: true
    charts:
      - name: keda
        version: "2.17.0"
        namespace: keda
        url: https://kedacore.github.io/charts
        repoName: keda
