apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent
spec:
  selector:
    matchLabels:
      app: agent
  template:
    metadata:
      labels:
        app: agent
    spec:
      containers:
        - name: agent
          image: ghcr.io/zarf-dev/zarf/agent:v0.38.1
      volume:
        - name: config-volume
          configMap:
            name: special-config
        - name: image
          image:
            reference: quay.io/crio/artifact:v1
            pullPolicy: IfNotPresent
