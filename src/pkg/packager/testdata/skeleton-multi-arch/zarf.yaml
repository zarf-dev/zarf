kind: ZarfPackageConfig

metadata:
  name: multi-arch-skeleton
  version: 0.0.1

components:
  - name: injector
    only:
      cluster:
        architecture: amd64

  - name: injector
    only:
      cluster:
        architecture: arm64
