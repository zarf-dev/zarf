kind: ZarfPackageConfig
metadata:
  name: manifest-with-package-values

values:
  files:
    - default-values.yaml

components:
  - name: test
    manifests:
      - name: test-manifest
        template: true
        files:
          - deployment.yaml
