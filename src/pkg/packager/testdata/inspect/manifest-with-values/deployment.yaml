apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
        - name: httpd
          image: httpd:{{ .Values.imageTag }}
          ports:
            - containerPort: {{ .Values.port }}
