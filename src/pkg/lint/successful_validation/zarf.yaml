# Plan to have the schema or the lint code allow these soon
# x-env: &env
#   environment:
#     - CONFIG_KEY
#     - EXAMPLE_KEY

# x-version: &version
#   version: 1.0.0

kind: ZarfPackageConfig
metadata:
  name: hello
  version: &version 1.0.0

components:
  - name: c1
    required: true
    charts:
      - name: redis
        namespace: redis-system
        version: *version
        localPath: ./redis
    images:
      - &hello_image docker.io/library/redis:latest
