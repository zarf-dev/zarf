kind: ZarfPackageConfig
metadata:
  name: distro-eks
  description: Deploy a EKS K8s cluster
  version: 0.0.4

variables:
  - name: EKS_CLUSTER_NAME
    description: The name for the EKS cluster
    prompt: true

  - name: EKS_INSTANCE_TYPE
    description: The EC2 instance type for the worker nodes
    default: t3.small

  - name: EKS_CLUSTER_REGION
    description: The AWS region to setup the cluster and associated networking
    default: us-east-1

  - name: PERMISSIONS_BOUNDARY_ARN
    description: The ARN of the IAM permissions boundary to apply to the cluster resources

  - name: PERMISSIONS_BOUNDARY_TAG
    description: The tag to apply to the cluster resources to indicate the permissions boundary

components:
  - name: load-eksctl
    required: true
    files:
      - source: eks.yaml
        target: eks.yaml
      - source: https://github.com/eksctl-io/eksctl/releases/download/v0.192.0/eksctl_Darwin_amd64.tar.gz
        target: binaries/eksctl_Darwin_x86_64
        executable: true
        # shasum: 48f6e71dc73b213e11b9f56dc46a6461db18239b9577be503c753fc2a6b86f65
        extractPath: eksctl
      - source: https://github.com/eksctl-io/eksctl/releases/download/v0.192.0/eksctl_Darwin_arm64.tar.gz
        target: binaries/eksctl_Darwin_arm64
        executable: true
        # shasum: 5cb2ea7dd576e16ad65b25ae9527e8360addffc0c3e17c9f2ba8e2b67ff34927
        extractPath: eksctl
      - source: https://github.com/eksctl-io/eksctl/releases/download/v0.192.0/eksctl_Linux_amd64.tar.gz
        target: binaries/eksctl_Linux_x86_64
        executable: true
        # shasum: 37557bc38ae8fa46e01cf9642629903025b21fdd7f4c4f82f147ac238ea44067
        extractPath: eksctl
      - source: https://github.com/eksctl-io/eksctl/releases/download/v0.192.0/eksctl_Linux_arm64.tar.gz
        target: binaries/eksctl_Linux_arm64
        executable: true
        # shasum: 80301d8d13dd7bff363eb9d4418d6c948538abdc58042dbb90a767fe59383acc
        extractPath: eksctl

  - name: deploy-eks-cluster
    description: Create an EKS cluster!
    actions:
      onDeploy:
        before:
          - cmd: ./binaries/eksctl_$(uname -s)_$(uname -m) create cluster --dry-run -f eks.yaml
          - cmd: sleep 15
          - cmd: ./binaries/eksctl_$(uname -s)_$(uname -m) create cluster -f eks.yaml
          - cmd: ./binaries/eksctl_$(uname -s)_$(uname -m) utils write-kubeconfig -c ${ZARF_VAR_EKS_CLUSTER_NAME}

  - name: teardown-eks-cluster
    description: Delete the EKS cluster that this package was used to create.
    actions:
      onDeploy:
        before:
          - cmd: ./binaries/eksctl_$(uname -s)_$(uname -m) delete cluster -f eks.yaml --disable-nodegroup-eviction --wait
        after:
          # clean up after ourselves
          - cmd: rm -rf binaries
          - cmd: rm -f eks.yaml
