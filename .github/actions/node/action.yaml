name: node-cache
description: "Node cache"

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 18
        cache: "npm"
        cache-dependency-path: "src/ui/package-lock.json"
