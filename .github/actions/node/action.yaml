name: node-cache
description: "Node cache"

runs:
  using: composite
  steps:
    - name: "Dependency: Setup Node"
      uses: actions/setup-node@v3
      with:
        node-version: 16
        cache: "npm"
